Here is an explanation of the given Python function:

- The function is named `sum`.
- It takes two arguments: `a` and `b`, both of which are expected to be integers.
- The function has a return type annotation `-> int`, indicating that it is expected to return an integer.
- The function body consists of a single line of code: `return a + b`.
- The line `return a + b` adds the values of `a` and `b` together and returns the result.
- The intention of the author is to provide a simple function that calculates the sum of two integers.